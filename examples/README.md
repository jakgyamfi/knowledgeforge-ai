# Safe test workflow

Do not add real recordings or transcripts here. To test the pipeline, place a short, non-sensitive audio file in `Inbox` or `recordings`. Run `knowledgeforge batch`, confirm the result under `transcripts`, and then remove the test audio and transcript if they are no longer needed.

All of those private folders and common audio/transcript formats are excluded by `.gitignore`.
